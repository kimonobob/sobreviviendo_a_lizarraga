// La pantalla de línea de tiempo vive en /timeline (componente + datos + estilos).
// Este módulo solo la expone con el nombre que consume App.jsx.
export { Timeline as TimelineView } from "../../timeline/Timeline";
